services:
  # Optional: ClickHouse for high-throughput experiments
  # Uncomment to enable
  # clickhouse:
  #   image: clickhouse/clickhouse-server:latest
  #   ports:
  #     - "8123:8123"
  #     - "9000:9000"
  #   volumes:
  #     - clickhouse_data:/var/lib/clickhouse
  #   environment:
  #     CLICKHOUSE_DB: siem
  #     CLICKHOUSE_USER: siem_user
  #     CLICKHOUSE_PASSWORD: siem_pass

  # Optional: Elasticsearch for alternative backend
  # elasticsearch:
  #   image: docker.elastic.co/elasticsearch/elasticsearch:8.9.0
  #   environment:
  #     - discovery.type=single-node
  #     - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
  #   ports:
  #     - "9200:9200"
  #   volumes:
  #     - es_data:/usr/share/elasticsearch/data

volumes:
  clickhouse_data:
  es_data: